using UnityEngine;
using System.Collections;
//JS -Removed for flex HW purposes
public class DeviceOrientationChecker : MonoBehaviour {
	//private bool ShouldCheckOrientation = false;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
/*		if (ShouldCheckOrientation == true) {
			if (Input.deviceOrientation == DeviceOrientation.Portrait) {
				Application.LoadLevel(1);
				ShouldCheckOrientation = false;
			}
			if(Input.GetKeyDown(KeyCode.Backspace)){
				Application.LoadLevel(1);
				ShouldCheckOrientation = false;
			}
		}*/
	}

	/*	void OnLevelWasLoaded(int level) {
		if (level == 0 || level == 1) {
			ShouldCheckOrientation = false;
		} else {
			ShouldCheckOrientation = true;
		}
	}*/
}